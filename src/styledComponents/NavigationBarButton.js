// As the name suggests, represents the styled component for the NavigationBarButtons

import styled from 'styled-components';

const NavigationBarButton = styled.span`
    font-family: inherit;
    font-size: 20px;
    font-weight: 700;
    color: white;
    padding-left: 20px;
    padding-right: 20px;
    cursor: pointer;
` ;

export default NavigationBarButton;
